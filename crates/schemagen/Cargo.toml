[package]
name = "schemagen"
version = "0.1.0"
edition = "2024"
publish = false

[dependencies]
dprint-plugin-nix = { path = "../.." }

[build-dependencies]
dprint-plugin-nix = { path = "../.." }

[dev-dependencies]
jsonschema = { version = "0.57.0", default-features = false }
serde_json = { version = "1.0" }
